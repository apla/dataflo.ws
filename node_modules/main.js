module.exports = {
	throwUnlessEqual: function () {
		if (arguments[0] == arguments[1])
			return arguments[0]
		else {
			console.log (arguments[0], ' == ', arguments[1]);
			throw ('arguments not equal');
		}
	},
	throwUnlessDefined: function () {
		if (typeof arguments[0] == "undefined")
			throw ('not defined');
	},
	throwNow: function () {
		throw ('defined');
	},
	handleGet: function (params) {
		// http server test:
		// 1) just request, check for User-Agent
		// 2) get request with query string
		// 3) post request
		if (params.cookieAndRedirect) {
			return true;
		}
		// if (checkCookie) {

		// }
	}
};